# Environment Setup (Windows)

This project was developed and tested on **Windows** using **Python 3.10**.
It uses:
- Intel RealSense depth camera (built from the librealsense GitHub repository)
- MediaPipe for vision-based person detection
- Serial communication for mmWave radar

---

## Python Version
- Python 3.10 recommended

Check:
```bash
python --version
